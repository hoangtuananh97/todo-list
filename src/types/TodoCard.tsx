export interface TodoCardType {
    id: string,
    text: string,
    cardTitle: string,
    status: string
}

export interface TodoCardTypeResponse {
    id: string,
    text: string,
    cardTitle: string,
    index: number
    idList: string
}